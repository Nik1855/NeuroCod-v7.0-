<Page
    x:Class="NeuroCodUI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Панель навигации -->
        <NavigationView Grid.Row="0" PaneDisplayMode="Top">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Diagnostics" Tag="diagnostics"/>
                <NavigationViewItem Content="AI Generator" Tag="ai_generator"/>
                <NavigationViewItem Content="Monitoring" Tag="monitoring"/>
                <NavigationViewItem Content="Settings" Tag="settings"/>
            </NavigationView.MenuItems>
        </NavigationView>
        
        <!-- Контент -->
        <Frame x:Name="ContentFrame" Grid.Row="1"/>
    </Grid>
</Page>